{
    "name": "GraphAppMsj-Api",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "node",
    "workspaceFolder": "/app/code",  // Directorio donde se abrirá la terminal
    "customizations": {
      "vscode": {
        "settings": {
          "terminal.integrated.cwd": "/app/code"  // Directorio inicial para la terminal
        },
        "extensions": [
            "ms-vscode-remote.remote-containers"
        ]
      }
    }
}

  